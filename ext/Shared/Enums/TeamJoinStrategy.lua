TeamJoinStrategy = {
    -- Play as solo even in team-based modes
    NoJoin = 1,

    -- Join a random team
    AutoJoin = 2,

    -- Join (or invite others) a custom team using its code (team id)
    Custom = 3
}
